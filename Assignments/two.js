//Check if input variable is a number or not.
let x = "25";
type = typeof x; //typeof type=string

if (type == "number") {
  console.log(`${x} is a number.`);
} else {
  console.log(`${x} is not a number.`);
}
